
import 'package:flutter/material.dart';

/// みんなが投稿したメッセージ一覧が表示されるページ
class TopPage extends StatelessWidget {
  const TopPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Top Page');
  }
}
